# Rust build artifacts
/target/

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# aurora-lint specific
.todo-sqlite-cli
.env
.env.*
# ...but the benchmark-config template is tracked on purpose (see
# docs/benchmark-setup.rst); it holds no secrets, only local paths.
!.env.example
aurora-lint-results.json
sqc-results.json
results/
*.log
test-summary.md
rules_templates/cwe/
data/benchmarks.db
data/benchmarks.db-wal
data/benchmarks.db-shm
# Task tracking moved to a coordinator-owned, non-git-tracked db spanning
# all three project repos (see ~/BECOME_COORDINATOR.md / ~/BECOME_WORKER.md,
# 2026-09-09) -- a stranger cloning this repo who wants their own local task
# list still gets one via `todo-sqlite-cli init`, same as data/benchmarks.db.
todo-sqlite-cli.db
todo-sqlite-cli.db-wal
todo-sqlite-cli.db-shm
todo-sqlite-cli.local.db
todo-sqlite-cli.local.db-wal
todo-sqlite-cli.local.db-shm

# Python
__pycache__/
*.pyc

# Coverage and test output
lcov.info
test-results.xml
test-output.xml

# Temporary files
*.tmp
*.temp
tmp/
docs/test-summary.md

# Generated module registration files
**/mod.rs
/docs/test-summary.md
data/prescan_cache/

# Sphinx build output
docs/_build/

# Claude Code runtime state
.claude/scheduled_tasks.lock

# Local MCP server configuration (contains org-specific paths and credentials)
.mcp.json

# todo-sqlite-cli native MQTT sync: node-local connection config and
# mailbox/replica state, not shared via git (see COORDINATOR_HANDOFF.md)
.todo-mqtt-*.json
*.mqtt-*.json
todo-sqlite-cli-replica.db
todo-sqlite-cli-replica.db-wal
todo-sqlite-cli-replica.db-shm

# Personal-network / local machine setup notes (not aurora-lint tooling)
BENCHMARK_PULLING.md
PARALLEL_NODES_ISSUES.md

# gavel local review-session state (task 637) -- churns per review action,
# not a shared artifact; only the batch JSON going in and the exported
# verdicts.json coming out are meant to be committed/imported
data/precision_audit/*/.gavel/
data/precision_audit/*/gavel.db

# Security disclosure packages: embargoed/unfixed vuln reports addressed to
# upstream maintainers. Kept on disk, never committed. (Swept into git by
# mistake 2026-09-02; removed from tip in the same session.)
data/precision_audit/*/disclosure_package/
data/precision_audit/*/*-disclosure-package-*.zip
