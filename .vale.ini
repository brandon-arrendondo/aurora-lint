# Vale prose lint for README.md and docs/ -- run `invoke lint-docs`. The rules
# are the Aurora house style from a sibling ../style_package checkout (the
# task builds its zip when missing); advisory only, not a gate.
StylesPath = styles
Packages = ../style_package/dist/Aurora.zip
